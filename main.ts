function multi(a: number, b:number): number {
    return a * b;
}

function concat(nome: string): string {
    return `Olá ${nome}! Como você está?`
}

console.log(multi(10,20));
console.log(concat('Rafael'));